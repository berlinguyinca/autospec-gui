export default function Home() {
  return (
    <main className="shell">
      <section className="intro">
        <p className="eyebrow">autospec-gui</p>
        <h1>Autospec telemetry dashboard</h1>
        <p>
          Initial scaffold for a read-only Next.js dashboard backed by the
          configured autospec Postgres telemetry database.
        </p>
      </section>
    </main>
  );
}
